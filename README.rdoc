== README

* Rails version >= 4.2.4

* Ruby version >= 2.1.6

* Configuration: check database.yml.example, settings.yml.example and .env.example. API_USER and API_PASSWORD are obtained through registration in api_provider app.

* Database creation and seeds: bundle exec rake db:setup.

* Specs: bundle exec rspec spec/